apiVersion: v1
kind: Secret
metadata:
  name: demo-secret
type: Opaque
stringData:
  # Example only -- never commit real secrets. Use SealedSecrets / External Secrets in production.
  DB_PASSWORD: change-me
  API_KEY: change-me
